services:
  dira:
    build:
      target: dev
    container_name: dira-dev
    command: npm run start
    volumes:
      - .:/app
      - /app/node_modules   # ← anonymous volume shields Linux node_modules
    ports:
      - "5544:5544" 
    environment:
      - NODE_ENV=development